{
  "$comment": "generated, do not edit; regenerate via: uv run python scripts/export_agent_schemas.py",
  "$defs": {
    "Contributes": {
      "description": "All contribution arrays for a single manifest.\n\n``memory_backends``, ``tools``, ``hooks``, ``services``, ``tool_gates``,\n``session_observers`` and ``onboard`` are consumed today; the model keeps\nextra fields silently so future contribution types don't break older hosts\nreading newer manifests.",
      "properties": {
        "hooks": {
          "items": {
            "$ref": "#/$defs/HookContribution"
          },
          "title": "Hooks",
          "type": "array"
        },
        "memory_backends": {
          "items": {
            "$ref": "#/$defs/MemoryBackendContribution"
          },
          "title": "Memory Backends",
          "type": "array"
        },
        "onboard": {
          "items": {
            "$ref": "#/$defs/OnboardContribution"
          },
          "title": "Onboard",
          "type": "array"
        },
        "services": {
          "items": {
            "$ref": "#/$defs/ServiceContribution"
          },
          "title": "Services",
          "type": "array"
        },
        "session_observers": {
          "items": {
            "$ref": "#/$defs/SessionObserverContribution"
          },
          "title": "Session Observers",
          "type": "array"
        },
        "tool_gates": {
          "items": {
            "$ref": "#/$defs/ToolGateContribution"
          },
          "title": "Tool Gates",
          "type": "array"
        },
        "tools": {
          "items": {
            "$ref": "#/$defs/ToolContribution"
          },
          "title": "Tools",
          "type": "array"
        }
      },
      "title": "Contributes",
      "type": "object"
    },
    "HookContribution": {
      "description": "One ``[[plugin.contributes.hooks]]`` entry.\n\n``factory`` is a ``module.path:callable`` resolving to a\n``Callable[[PluginContext], AgentHook]`` -- it returns one\n:class:`~raven.contracts.loop_hooks.AgentHook` the host appends to the\nloop's hook chain at boot. This is how product code steers the loop\n(budget notes, forced finalization, spin breaking) from a plugin\ndirectory instead of a fork: the loop fires the phases, the plugin\ndecides on them.",
      "properties": {
        "factory": {
          "minLength": 1,
          "title": "Factory",
          "type": "string"
        },
        "name": {
          "minLength": 1,
          "title": "Name",
          "type": "string"
        }
      },
      "required": [
        "name",
        "factory"
      ],
      "title": "HookContribution",
      "type": "object"
    },
    "MemoryBackendContribution": {
      "description": "One ``[[plugin.contributes.memory_backends]]`` entry.",
      "properties": {
        "factory": {
          "minLength": 1,
          "title": "Factory",
          "type": "string"
        },
        "name": {
          "minLength": 1,
          "title": "Name",
          "type": "string"
        }
      },
      "required": [
        "name",
        "factory"
      ],
      "title": "MemoryBackendContribution",
      "type": "object"
    },
    "OnboardContribution": {
      "description": "One ``[[plugin.contributes.onboard]]`` entry.\n\n``factory`` is a ``module.path:callable`` resolving to a\n``Callable[[PluginContext], OnboardStep]`` -- it returns one object\nsatisfying :class:`~raven.contracts.onboard.OnboardStep`. ``raven\nonboard`` lends the wizard shell as one ``OnboardUI`` and records the\nreturned ``StepOutcome``; the plugin never writes ``memory.backend``\nitself (paper: contracts/onboard.py).",
      "properties": {
        "factory": {
          "minLength": 1,
          "title": "Factory",
          "type": "string"
        },
        "name": {
          "minLength": 1,
          "title": "Name",
          "type": "string"
        }
      },
      "required": [
        "name",
        "factory"
      ],
      "title": "OnboardContribution",
      "type": "object"
    },
    "ServiceContribution": {
      "description": "One ``[[plugin.contributes.services]]`` entry.\n\n``factory`` is a ``module.path:callable`` resolving to a\n``Callable[[PluginContext], PluginService]`` -- it returns one object\nsatisfying :class:`~raven.contracts.services.PluginService`. Only a\nresident host starts it; the host owns the lifecycle, and the paper pins\nthe two disciplines (no assembly mutation, loud stop on error).",
      "properties": {
        "factory": {
          "minLength": 1,
          "title": "Factory",
          "type": "string"
        },
        "name": {
          "minLength": 1,
          "title": "Name",
          "type": "string"
        }
      },
      "required": [
        "name",
        "factory"
      ],
      "title": "ServiceContribution",
      "type": "object"
    },
    "SessionObserverContribution": {
      "description": "One ``[[plugin.contributes.session_observers]]`` entry.\n\n``factory`` is a ``module.path:callable`` resolving to a\n``Callable[[PluginContext], SessionObserver]`` -- it returns one object\nsatisfying :class:`~raven.contracts.session_events.SessionObserver`, or\n``None`` to decline (nothing to release means no observer). A resident\nhost attaches the built observers to the session store for the\ngeneration; the paper pins the three disciplines (the store notifies and\nnever waits, the request and its outcome, in-process only).",
      "properties": {
        "factory": {
          "minLength": 1,
          "title": "Factory",
          "type": "string"
        },
        "name": {
          "minLength": 1,
          "title": "Name",
          "type": "string"
        }
      },
      "required": [
        "name",
        "factory"
      ],
      "title": "SessionObserverContribution",
      "type": "object"
    },
    "ToolContribution": {
      "description": "One ``[[plugin.contributes.tools]]`` entry.\n\n``factory`` is a ``module.path:callable`` resolving to a\n``Callable[[PluginContext], Tool]`` \u2014 it returns a single\n:class:`~raven.contracts.tool.Tool` the host registers into the\nagent's tool set at boot. One tool per entry; a plugin exposing\nseveral tools lists several entries.",
      "properties": {
        "factory": {
          "minLength": 1,
          "title": "Factory",
          "type": "string"
        },
        "name": {
          "minLength": 1,
          "title": "Name",
          "type": "string"
        }
      },
      "required": [
        "name",
        "factory"
      ],
      "title": "ToolContribution",
      "type": "object"
    },
    "ToolGateContribution": {
      "description": "One ``[[plugin.contributes.tool_gates]]`` entry.\n\n``factory`` is a ``module.path:callable`` resolving to a\n``Callable[[PluginContext], ToolGate]`` -- it returns one object\nsatisfying :class:`~raven.contracts.tool_gate.ToolGate`, or ``None`` to\ndecline (no policy configured means no gate). The host casts the built\ngates over the agent's tool registry at assembly; the paper pins the\nthree disciplines (cast at assembly, a verdict replaces one call, an\nerror refuses it).",
      "properties": {
        "factory": {
          "minLength": 1,
          "title": "Factory",
          "type": "string"
        },
        "name": {
          "minLength": 1,
          "title": "Name",
          "type": "string"
        }
      },
      "required": [
        "name",
        "factory"
      ],
      "title": "ToolGateContribution",
      "type": "object"
    }
  },
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "description": "A plugin manifest: everything lives under the [plugin] table, mirroring PluginManifest.from_toml_path, which unwraps that one key before validating. Exported from raven.plugins.manifest.PluginManifest.",
  "properties": {
    "plugin": {
      "description": "Parsed ``raven-plugin.toml``.\n\nConstructed via :meth:`from_toml_path` / :meth:`from_toml_str`; the\nraw ``__init__`` works too for programmatic tests.",
      "properties": {
        "bundled": {
          "default": false,
          "title": "Bundled",
          "type": "boolean"
        },
        "config_schema": {
          "additionalProperties": true,
          "title": "Config Schema",
          "type": "object"
        },
        "contributes": {
          "$ref": "#/$defs/Contributes"
        },
        "display_name": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Display Name"
        },
        "id": {
          "minLength": 1,
          "title": "Id",
          "type": "string"
        },
        "raven": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Raven"
        },
        "version": {
          "minLength": 1,
          "title": "Version",
          "type": "string"
        }
      },
      "required": [
        "id",
        "version"
      ],
      "title": "PluginManifest",
      "type": "object"
    }
  },
  "required": [
    "plugin"
  ],
  "title": "raven-plugin.toml",
  "type": "object"
}
