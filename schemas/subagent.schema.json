{
  "$comment": "generated, do not edit; regenerate via: uv run python scripts/export_agent_schemas.py",
  "$defs": {
    "SubagentEngineConfig": {
      "description": "The engine wheel a folder-discovered agent declares, as\n``{\"package\": \"<import name>\", \"wheel\": \"<distribution name>\"}``.\n\nDeclared by the folder's ``subagent.json`` (the ``raven agents new\n--engine-wheel`` scaffold writes it); readiness probes the package where\nraven runs and lists the row disabled, naming the wheel, until it imports.\nCarried on the config row so a pinned copy of the manifest round-trips the\ndeclaration instead of silently dropping it -- discovery itself keeps\nreading the manifest fresh (``vendored_agents._declared_engine``), so this\nfield changes no merge or readiness semantics.",
      "properties": {
        "package": {
          "default": "",
          "title": "Package",
          "type": "string"
        },
        "wheel": {
          "default": "",
          "title": "Wheel",
          "type": "string"
        }
      },
      "title": "SubagentEngineConfig",
      "type": "object"
    },
    "SubagentMemoryConfig": {
      "additionalProperties": true,
      "description": "How the host addresses one sub-agent's memories.\n\nOpaque to the host except the two keys below: a sub-agent runs in a\nprocess of its own, and whatever identifies its memories is the memory\nbackend's vocabulary, not raven's. This block is handed to the backend as\nit stands -- ``store``'s per-call ``user_id`` / ``agent_id`` override for\na trace, and ``recall_session``'s track for the read back. EverOS reads\n``user_id`` and ``agent_id``; another backend may want something else, and\nthe host has no business validating either.\n\nDeclared rather than discovered. The alternative -- reading the fork's own\nconfig.json next to its ``run.py`` -- would couple the host to a directory\nconvention that lives entirely inside each fork.\n\nThe old ``everos`` spelling still loads. Its ``base_url`` does not: no\nconfig, fixture or document ever set one, and honouring it would mean\nevery backend growing a per-call way to address a different server.",
      "properties": {
        "sessionPrefix": {
          "default": "cli:",
          "title": "Sessionprefix",
          "type": "string"
        },
        "source": {
          "default": "agent",
          "enum": [
            "agent",
            "trace"
          ],
          "title": "Source",
          "type": "string"
        }
      },
      "title": "SubagentMemoryConfig",
      "type": "object"
    },
    "SubagentRouteConfig": {
      "description": "One candidate target offered to the host's route classifier.\n\nBeyond the target's name, a route says what the work is owed and what the\nrow's own implementation is to be told when the route is closed to a\ndispatch. Both are the *row's* words, not the gate's: the gate that closes a\nroute is one piece of code serving every row that declares one, and prose\nabout a deck written into it would be appended to the next row's requests\nfor something else entirely.",
      "properties": {
        "minTier": {
          "default": "",
          "title": "Mintier",
          "type": "string"
        },
        "needs": {
          "default": [],
          "items": {
            "type": "string"
          },
          "title": "Needs",
          "type": "array"
        },
        "note": {
          "default": "",
          "title": "Note",
          "type": "string"
        },
        "noteFile": {
          "default": "",
          "title": "Notefile",
          "type": "string"
        },
        "owes": {
          "default": "",
          "title": "Owes",
          "type": "string"
        },
        "to": {
          "title": "To",
          "type": "string"
        }
      },
      "required": [
        "to"
      ],
      "title": "SubagentRouteConfig",
      "type": "object"
    },
    "ThirdPartyAcpSubagentConfig": {
      "description": "A third-party agent reached over ACP (Agent Client Protocol), e.g. ``hermes acp``.\n\n``command`` starts a *server* and is spawned once per connection, not once\nper task: a task is delivered as a ``session/prompt`` request on the running\nconnection. So unlike a cli entry it carries no ``{prompt}`` placeholder,\nand none of the fields listed in :data:`ACP_UNSUPPORTED_FIELDS`.\n\nThose seven are absent by design rather than by omission. Each one is a cli\n*declaration* about behaviour (can it resume, who mints the session id, how\nis its transcript shaped) whose acp counterpart comes from the ``initialize``\nhandshake instead. Accepting both would make every one of them a second\nsource of truth, and the first time a declaration disagreed with the\nhandshake nothing in the code would know which to believe.",
      "properties": {
        "allowMcpSecrets": {
          "default": false,
          "title": "Allowmcpsecrets",
          "type": "boolean"
        },
        "command": {
          "title": "Command",
          "type": "string"
        },
        "cwd": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Cwd"
        },
        "description": {
          "default": "",
          "title": "Description",
          "type": "string"
        },
        "enabled": {
          "default": true,
          "title": "Enabled",
          "type": "boolean"
        },
        "engine": {
          "anyOf": [
            {
              "$ref": "#/$defs/SubagentEngineConfig"
            },
            {
              "type": "null"
            }
          ],
          "default": null
        },
        "env": {
          "additionalProperties": {
            "type": "string"
          },
          "title": "Env",
          "type": "object"
        },
        "hidden": {
          "default": false,
          "title": "Hidden",
          "type": "boolean"
        },
        "kind": {
          "const": "acp",
          "default": "acp",
          "title": "Kind",
          "type": "string"
        },
        "maxOutputChars": {
          "default": 30000,
          "title": "Maxoutputchars",
          "type": "integer"
        },
        "mcps": {
          "anyOf": [
            {
              "items": {
                "type": "string"
              },
              "type": "array"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Mcps"
        },
        "memory": {
          "anyOf": [
            {
              "$ref": "#/$defs/SubagentMemoryConfig"
            },
            {
              "type": "null"
            }
          ],
          "default": null
        },
        "name": {
          "title": "Name",
          "type": "string"
        },
        "owns": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Owns"
        },
        "ownsWatchedWork": {
          "default": false,
          "title": "Ownswatchedwork",
          "type": "boolean"
        },
        "preset": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Preset"
        },
        "readyTimeoutMs": {
          "default": 30000,
          "title": "Readytimeoutms",
          "type": "integer"
        },
        "routes": {
          "items": {
            "$ref": "#/$defs/SubagentRouteConfig"
          },
          "title": "Routes",
          "type": "array"
        },
        "sessionMcp": {
          "anyOf": [
            {
              "type": "boolean"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Sessionmcp"
        },
        "switchOnly": {
          "default": false,
          "title": "Switchonly",
          "type": "boolean"
        },
        "timeout": {
          "anyOf": [
            {
              "type": "integer"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Timeout"
        }
      },
      "required": [
        "name",
        "command"
      ],
      "title": "ThirdPartyAcpSubagentConfig",
      "type": "object"
    },
    "ThirdPartyCliSubagentConfig": {
      "description": "A third-party CLI agent (claude code, codex, \u2026) callable as a native subagent.\n\n``command`` is an argv template: a ``{prompt}`` token is replaced by the task\ntext as a single argv token (injection-safe), ``{prompt_file}`` by a path to\na file holding the prompt. With neither placeholder, the prompt is delivered\non the child's stdin.\n\nSetting ``resume_command`` makes the agent stateful: a caller-supplied\ninstance handle is bound to the CLI's own session id, substituted as\n``{agent_id}``. With ``id_source=\"provisioned\"`` raven mints the id and\npasses it on the create call; with ``\"derived\"`` the CLI mints it and raven\nreads it back out of the transcript.\n\n``timeout`` is ``None`` by default, meaning no automatic limit: the run is\nended by hand (manual stop), not by a timer. Set it to opt into an\nautomatic backstop instead.",
      "properties": {
        "allowMcpSecrets": {
          "default": false,
          "title": "Allowmcpsecrets",
          "type": "boolean"
        },
        "command": {
          "title": "Command",
          "type": "string"
        },
        "cwd": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Cwd"
        },
        "description": {
          "default": "",
          "title": "Description",
          "type": "string"
        },
        "enabled": {
          "default": true,
          "title": "Enabled",
          "type": "boolean"
        },
        "engine": {
          "anyOf": [
            {
              "$ref": "#/$defs/SubagentEngineConfig"
            },
            {
              "type": "null"
            }
          ],
          "default": null
        },
        "env": {
          "additionalProperties": {
            "type": "string"
          },
          "title": "Env",
          "type": "object"
        },
        "idSource": {
          "default": "provisioned",
          "enum": [
            "provisioned",
            "derived"
          ],
          "title": "Idsource",
          "type": "string"
        },
        "kind": {
          "const": "cli",
          "default": "cli",
          "title": "Kind",
          "type": "string"
        },
        "maxOutputChars": {
          "default": 30000,
          "title": "Maxoutputchars",
          "type": "integer"
        },
        "mcps": {
          "anyOf": [
            {
              "items": {
                "type": "string"
              },
              "type": "array"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Mcps"
        },
        "memory": {
          "anyOf": [
            {
              "$ref": "#/$defs/SubagentMemoryConfig"
            },
            {
              "type": "null"
            }
          ],
          "default": null
        },
        "name": {
          "title": "Name",
          "type": "string"
        },
        "outputPattern": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Outputpattern"
        },
        "owns": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Owns"
        },
        "ownsWatchedWork": {
          "default": false,
          "title": "Ownswatchedwork",
          "type": "boolean"
        },
        "preset": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Preset"
        },
        "readsLocalFiles": {
          "default": true,
          "title": "Readslocalfiles",
          "type": "boolean"
        },
        "resumeCommand": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Resumecommand"
        },
        "sessionIdPattern": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Sessionidpattern"
        },
        "stateful": {
          "anyOf": [
            {
              "type": "boolean"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Stateful"
        },
        "switchOnly": {
          "default": false,
          "title": "Switchonly",
          "type": "boolean"
        },
        "timeout": {
          "anyOf": [
            {
              "type": "integer"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Timeout"
        },
        "transcriptFormat": {
          "default": "text",
          "enum": [
            "text",
            "codex_jsonl",
            "claude_stream_json",
            "openclaw_json",
            "opencode_json"
          ],
          "title": "Transcriptformat",
          "type": "string"
        }
      },
      "required": [
        "name",
        "command"
      ],
      "title": "ThirdPartyCliSubagentConfig",
      "type": "object"
    }
  },
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "description": "An agent folder's roster-row manifest, as discovered from agents/<folder>/subagent.json. Exported from raven.config.schema (ThirdPartyAcpSubagentConfig | ThirdPartyCliSubagentConfig).",
  "discriminator": {
    "mapping": {
      "acp": "#/$defs/ThirdPartyAcpSubagentConfig",
      "cli": "#/$defs/ThirdPartyCliSubagentConfig"
    },
    "propertyName": "kind"
  },
  "oneOf": [
    {
      "$ref": "#/$defs/ThirdPartyAcpSubagentConfig"
    },
    {
      "$ref": "#/$defs/ThirdPartyCliSubagentConfig"
    }
  ],
  "title": "subagent.json"
}
