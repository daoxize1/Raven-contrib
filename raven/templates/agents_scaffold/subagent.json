{
  "name": "My-Agent",
  "kind": "acp",
  "description": "TODO: one paragraph the dispatching model reads to decide when to pick this agent. Say what it does, when to use it, and what signals to look for.",
  "owns": "TODO: one clause completing 'My-Agent ...' about the work this agent owns.",
  "enabled": true,
  "command": "{PYTHON} {SUBAGENT_DIR}/run.py --acp",
  "cwd": "{SUBAGENT_DIR}",
  "readyTimeoutMs": 120000,
  "timeout": null,
  "maxOutputChars": 60000,
  "memory": {
    "userId": "my-agent",
    "agentId": "my-agent"
  }
}
